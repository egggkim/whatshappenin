<!DOCTYPE html>
<html>
<head>
  <title>Whatshappenin</title>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
   

  <%= stylesheet_link_tag  "custom-animations.css", "style.css", "bootstrap.css", "application.css", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "ie10-viewport-bug-workaround.js", "application.js", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
   
    <!-- GOOGLE MAPS API -->
    
    <script src="https://maps.googleapis.com/maps/api/js?v=3.exp&signed_in=true&libraries=places&key=#{ENV[API_KEY]}"></script>

    <script type="text/javascript">
      google.maps.event.addDomListener(window, 'load', initialize);
    </script>
    
</head>
<body>
<% if logged_in? %>
  <%= render 'shared/nav' %>
<% end %>
  <%= yield %>

<% if logged_in? %>
  <%= render 'shared/footer' %>
<% end %>
 <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
     <%= javascript_include_tag "jquery.min.js", "bootstrap.min.js", "retina-1.1.0.js", "jquery.unveilEffects.js" %>
</body>
</html>
